name: Deploy Test

on:
  push:
    branches: [develop]

jobs:
  deploy:
    runs-on: windows-test
    timeout-minutes: 30

    steps:
      - name: Deploy
        shell: cmd
        run: |
          cd /d D:\brandflow
          git pull origin develop
          call packaging\windows\deploy.bat
