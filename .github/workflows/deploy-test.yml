name: Deploy Test

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: windows-test
    timeout-minutes: 30

    steps:
      - name: Deploy
        shell: cmd
        run: |
          cd /d D:\brandflow
          git pull
          call packaging\windows\deploy.bat
