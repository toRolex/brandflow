name: Deploy Production

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: windows-prod
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Run deploy script
        shell: cmd
        run: packaging\windows\deploy.bat
        working-directory: ${{ github.workspace }}
