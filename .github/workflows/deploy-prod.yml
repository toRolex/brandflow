name: Deploy Production

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: windows-prod
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Sync code to production directory and deploy
        shell: cmd
        run: |
          xcopy /y /e /q "%CD%\*" "D:\视频生成工厂\ai-video-pipeline\" >nul
          cd /d D:\视频生成工厂\ai-video-pipeline
          call packaging\windows\deploy.bat
