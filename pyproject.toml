[project]
name = "ziyuantang-pipeline"
version = "0.1.0"
description = "Local short-video automation pipeline"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv",
    "requests",
    "fastapi",
    "uvicorn",
    "openpyxl",
    "pillow",
]
